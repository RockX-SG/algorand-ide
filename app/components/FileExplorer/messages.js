/*
 * FileExplorer Messages
 *
 * This contains all the text for the FileExplorer component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.FileExplorer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FileExplorer component!',
  },
});
