/*
 * OptInAsaForm Messages
 *
 * This contains all the text for the OptInAsaForm component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.OptInAsaForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the OptInAsaForm component!',
  },
});
