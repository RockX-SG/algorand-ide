/*
 * RouteSign Messages
 *
 * This contains all the text for the RouteSign component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.RouteSign';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RouteSign component!',
  },
});
