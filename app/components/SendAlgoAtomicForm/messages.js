/*
 * SendAlgoAtomicForm Messages
 *
 * This contains all the text for the SendAlgoAtomicForm component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SendAlgoAtomicForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SendAlgoAtomicForm component!',
  },
});
