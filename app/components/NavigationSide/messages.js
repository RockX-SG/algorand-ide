/*
 * NavigationSide Messages
 *
 * This contains all the text for the NavigationSide component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.NavigationSide';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NavigationSide component!',
  },
});
