/*
 * SendAlgoForm Messages
 *
 * This contains all the text for the SendAlgoForm component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SendAlgoForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SendAlgoForm component!',
  },
});
