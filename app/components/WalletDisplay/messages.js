/*
 * WalletDisplay Messages
 *
 * This contains all the text for the WalletDisplay component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.WalletDisplay';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WalletDisplay component!',
  },
});
