/*
 * AssetAsaDisplay Messages
 *
 * This contains all the text for the BashConsole component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AssetAsaDisplay';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AssetAsaDisplay component!',
  },
});
