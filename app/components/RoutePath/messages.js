/*
 * RoutePath Messages
 *
 * This contains all the text for the RoutePath component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.RoutePath';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RoutePath component!',
  },
});
