/*
 * CustomNetworkDisclaimer Messages
 *
 * This contains all the text for the CustomNetworkDisclaimer component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CustomNetworkDisclaimer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CustomNetworkDisclaimer component!',
  },
});
