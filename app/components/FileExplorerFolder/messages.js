/*
 * FileExplorerFolder Messages
 *
 * This contains all the text for the FileExplorerFolder component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.FileExplorerFolder';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FileExplorerFolder component!',
  },
});
