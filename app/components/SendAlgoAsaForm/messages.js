/*
 * SendAlgoAsaForm Messages
 *
 * This contains all the text for the SendAlgoAsaForm component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SendAlgoAsaForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SendAlgoAsaForm component!',
  },
});
