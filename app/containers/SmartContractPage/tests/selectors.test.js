// import { selectSmartContractPageDomain } from '../selectors';

describe('selectSmartContractPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
