/*
 * SmartContractPage Messages
 *
 * This contains all the text for the SmartContractPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SmartContractPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SmartContractPage container!',
  },
});
