// import { selectTutorialPageDomain } from '../selectors';

describe('selectTutorialPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
