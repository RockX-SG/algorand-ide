// import { selectExplorerPageDomain } from '../selectors';

describe('selectExplorerPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
