/*
 * ExplorerPage Messages
 *
 * This contains all the text for the ExplorerPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ExplorerPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ExplorerPage container!',
  },
});
