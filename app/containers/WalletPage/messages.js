/*
 * WalletPage Messages
 *
 * This contains all the text for the WalletPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.WalletPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WalletPage container!',
  },
});
