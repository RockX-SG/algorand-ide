// import { selectTutorialJsHashTimeLockPageDomain } from '../selectors';

describe('selectTutorialJsHashTimeLockPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
