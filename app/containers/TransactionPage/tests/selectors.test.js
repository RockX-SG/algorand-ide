// import { selectTransactionPageDomain } from '../selectors';

describe('selectTransactionPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
